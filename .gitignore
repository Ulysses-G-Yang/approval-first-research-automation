# Python bytecode and test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Local environments and secrets
.venv/
venv/
.uv-cache/
.uv-python/
.playwright-browsers/
.package-venv/
.env
.env.*
!.env.example
*.local.yaml

# Crawler output and local verification artifacts
output/
.demo-tasks/
output_*.json
output_*.jsonl
output_*.csv
douban_top250*.json
douban_top250*.jsonl
configs/*-broken-auto.yaml
configs/*-systemchrome.yaml

# Defense in depth for legacy local profile directories. v2.1 does not create
# or consume them, but an old checkout must never accidentally commit sessions.
scripts/publish_states/
scripts/browser_state/

# Repair memory and demo artifacts (local only)
*.demo_repairs.jsonl

# Logs and editor settings
*.log
.vscode/
.idea/
docs/assets/.frames/
